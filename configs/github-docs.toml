# jmunch-mcp config for 'github-docs' upstream.
# Registered in Claude Desktop

threshold_tokens = 2000
log_level = "INFO"
report = false

[upstream]
command = "python"
args = ["-m", "github_docs_mcp.server"]
